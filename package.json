{
	"scripts": {
		"start": "npm-run-all compile --parallel server compile:watch build:watch",
		"test": "eslint --cache --ext .js --ext .jsx *.js lib/components views && echo ✓",
		"server": "servedir ./dist/site",
		"build": "complate-ssg",
		"build:watch": "nodemon -w dist/views.js -w content -e md -x 'npm run build' < /dev/null",
		"build:prod": "npm-run-all compile:prod build",
		"compile": "faucet --compact",
		"compile:watch": "npm run compile -- --watch",
		"compile:prod": "npm run compile -- --fingerprint"
	},
	"dependencies": {
		"prismjs": "^1.15.0"
	},
	"devDependencies": {
		"commonmark": "^0.28.1",
		"complate-ssg": "^0.19.2",
		"eslint-config-fnd-jsx": "^1.4.0",
		"faucet-pipeline-sass": "^1.0.0-rc.4",
		"faucet-pipeline-static": "^1.0.0-rc.5",
		"metacolon": "^1.0.0",
		"nodemon": "^1.17.5",
		"normalize.css": "^8.0.0",
		"npm-run-all": "^4.1.3",
		"servedir": "^1.0.1"
	}
}
