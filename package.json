{
	"scripts": {
		"start": "npm-run-all compile --parallel server compile:watch build:watch",
		"test": "eslint --cache --ext .js --ext .jsx *.js lib/components views && echo ✓",
		"server": "servedir ./dist/site",
		"build": "node ./index.js",
		"build:watch": "nodemon --delay 1 -w dist/views.js -w content -e md -x 'node ./index.js' < /dev/null",
		"build:prod": "npm-run-all compile:prod build",
		"compile": "faucet --compact",
		"compile:watch": "npm run compile -- --watch",
		"compile:prod": "faucet --compact --fingerprint"
	},
	"dependencies": {
		"prismjs": "^1.13.0"
	},
	"devDependencies": {
		"commonmark": "^0.28.1",
		"complate-ssg": "^0.16.2",
		"eslint-config-fnd-jsx": "^1.3.0",
		"faucet-pipeline-sass": "^1.0.0-rc.2",
		"faucet-pipeline-static": "^1.0.0-rc.2",
		"metacolon": "^1.0.0",
		"nodemon": "^1.17.2",
		"normalize.css": "^8.0.0",
		"npm-run-all": "^4.1.2",
		"servedir": "^1.0.1"
	}
}
